export interface Customer {
    firstName: string;
    lastName: string;
    phone: string;
    arrivalDate: string;
    arrivalTime: string;
    departureTime: string;
    total: number;
}

export interface CustomerList {
    name: string,
    total: number,
}
